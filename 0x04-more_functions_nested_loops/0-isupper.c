#include "main.h"
/**
 * _isupper - checks for uppercase char
 * @c: function parameter
 * Return: ! if its uppercase
 */

int _ispper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	else	
		return (0);
}
